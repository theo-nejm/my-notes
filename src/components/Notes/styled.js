import styled from "styled-components";

export const NotesWrapper = styled.div`
  height: auto;
  width: 90vw;
  margin-left: 5vw;

  padding: 1rem 0;

  display: flex;
  flex-wrap: wrap;
  align-items: center;
  justify-content: center;

  gap: 1.5rem;

  h3 {
    font-size: 1.5rem;
  }
`
